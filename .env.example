# Keine API-Schlüssel erforderlich – CLI nutzt Google OAuth

# Optional: Debug-Modus
DEBUG=false

# Optional: Timeout-Einstellungen (in Millisekunden)
CLI_TIMEOUT=120000
VIDEO_TIMEOUT=300000

# Optional: CLI-Pfad (falls nicht global installiert)
GEMINI_CLI_PATH=gemini
